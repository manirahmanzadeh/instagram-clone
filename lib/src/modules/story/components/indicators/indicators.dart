
import 'package:flutter/material.dart';

class StoryIndicators extends StatelessWidget {
  const StoryIndicators({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
